import { PaperContainerDirective } from './paper-container.directive';

describe('PaperContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new PaperContainerDirective();
    expect(directive).toBeTruthy();
  });
});
